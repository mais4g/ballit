<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalhes da Partida</title>
</head>
<body>
    <h1>Detalhes da Partida</h1>
    <p>
        <strong><%= match.teamA.nome %></strong> (Pontos: <%= match.scoreA %>) 
        VS 
        <strong><%= match.teamB.nome %></strong> (Pontos: <%= match.scoreB %>)
    </p>

    <% if (!match.completed) { %>
        <h2>Ações</h2>
        <form action="/match/<%= match._id %>/update" method="POST">
            <button type="submit" name="action" value="teamA_plif"><%= match.teamA.nome %> +5 (Plif)</button>
            <button type="submit" name="action" value="teamA_adv"><%= match.teamA.nome %> +3 (Adv)</button>
            <button type="submit" name="action" value="teamA_blot"><%= match.teamA.nome %> -2 (Blot)</button>
            <br>
            <button type="submit" name="action" value="teamB_plif"><%= match.teamB.nome %> +5 (Plif)</button>
            <button type="submit" name="action" value="teamB_adv"><%= match.teamB.nome %> +3 (Adv)</button>
            <button type="submit" name="action" value="teamB_blot"><%= match.teamB.nome %> -2 (Blot)</button>
        </form>

        <form action="/match/<%= match._id %>/end" method="POST" style="margin-top: 20px;">
            <button type="submit">Encerrar Partida</button>
        </form>
    <% } else { %>
        <p>Esta partida já foi encerrada.</p>
    <% } %>

    <a href="/start">Voltar</a>
</body>
</html>
